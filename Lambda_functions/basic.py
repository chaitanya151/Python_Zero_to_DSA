# lambda arguments: expression

square = lambda x: x * x
# print(f"Square : {square(5)}")

add = lambda x, y: x + y
# print(f"add : {add(8, 3)}")

# Create a lambda function that takes two numbers and returns their product.
product = lambda x, y: x * y
# print(f"product : {product(8, 2)}")

# Create a lambda function that takes three numbers and returns their average.
avg = lambda x, y, z: (x + y + z) / 3
# print(f"avg : {avg(10, 10, 10)}")
