# Mutable, Unique elements, unordered
# Immutable are pass by values. Mutables are pass by reference.
# Set can't contain list as itls mutable only immutables (tuple will work)

my_set = {1, 2, 33, 45, 6, 6, 6, 7, 8, 8, "Goku", "Vegita", 88.76}
print(my_set)
print(type(my_set))
